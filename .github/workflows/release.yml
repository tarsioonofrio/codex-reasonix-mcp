name: Release npm package

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  publish:
    name: Trusted publish
    runs-on: ubuntu-24.04
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.release.tag_name }}
      - name: Install pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 10.33.2
          run_install: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Install bubblewrap (command sandbox)
        run: sudo apt-get update && sudo apt-get install -y bubblewrap
      - name: Allow bwrap user namespaces (Ubuntu 24.04 AppArmor)
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
      - run: pnpm install --frozen-lockfile
      - id: release
        name: Validate release tag and select npm dist-tag
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: node scripts/release-metadata.mjs --github-output
      - run: pnpm check
      - run: pnpm audit --audit-level high
      - run: mkdir -p release-artifacts
      - run: pnpm pack --pack-destination release-artifacts
      - uses: actions/upload-artifact@v7
        with:
          name: codex-reasonix-mcp-${{ steps.release.outputs.version }}
          path: release-artifacts/*.tgz
          if-no-files-found: error
      - name: Publish with npm trusted publishing and provenance
        run: npm publish --provenance --access public --tag ${{ steps.release.outputs.dist_tag }}
